# Highlighter 2.1 范围冻结与候选基线

- **冻结日期：** 2026-08-09
- **冻结提交：** `1a57468188cbbeac949034462512ca10e9062577`
- **开发版本：** `2.1.0-beta.0`
- **目标平台：** Windows 10/11 x64
- **发布形态：** NSIS 安装版与 portable 便携版

`2.1.0-beta.0` 是内部稳定化版本标识，不代表已经发布公开 Beta。公开 Beta 必须满足本文门禁并使用新的版本提交。

## 1. 产品定位

2.1 将 Highlighter 收敛为 Windows 本地优先的截图、OCR、划词与 AI 后处理工具。本阶段不继续扩大功能面，优先完成可信发布、安全边界、诊断和真实环境验证。

## 2. 冻结范围

2.1 只接受以下工作：

- 修复冻结基线上能够复现的正确性、稳定性、性能或兼容性问题。
- 完善版本、CI、签名、Release、安装和回滚门禁。
- 收口 preload、CSP、导航、新窗口、IPC sender/owner 和生产包完整性边界。
- 增加本地诊断、异常退出记录和脱敏验证。
- 为降低回归风险而进行的等价模块拆分；重构提交不得混入产品行为变化。
- 补齐真实 Electron renderer、NSIS/portable 和 Windows 硬件验收。

2.1 不接受以下工作：

- 新增截图、标注、OCR、长截图、贴图或录屏功能。
- 新增 AI 动作、模型能力、服务商或提示词功能。
- 录屏音频、跨平台、云同步、团队协作或插件市场。
- 不服务于上述门禁的大规模 UI 改版或数据格式迁移。

OCR 历史全文搜索、剪贴板兼容模式和 AI 服务商抽象保留为 2.1 稳定发布后的候选项，必须分别评审和提交。

## 3. 冻结时自动化基线

在冻结提交的依赖环境中验证：

| 检查 | 结果 |
|---|---|
| `npm run check` | 103 个 JavaScript 文件通过 |
| `npm test` | 264 passed，0 failed |
| `npm run test:long-capture` | 通过 |
| `npm run check:version` | 2.1.0-beta.0 通过 |
| `npm run test:runtime` | 2 passed，0 failed |

runtime probe 只证明 Electron 能加载原生模块、组件路径和显示器 API，不证明真实 renderer、安装、升级或完整业务流程通过。

## 4. 当前工程面

| 项目 | 冻结值 |
|---|---:|
| `BrowserWindow` 创建点 | 10 |
| 本地 HTML 页面 | 10 |
| `ipcMain.handle/on` 注册点 | 93 |
| `main.js` | 2922 行 |
| `config/config.js` | 828 行 |

完整安全验收必须覆盖全部窗口、页面和 IPC 注册点。模块拆分优先选择高变更的窗口创建、截图编排和划词流程，但不得以拆分完成替代运行时验证。

## 5. 2.1 发布门禁

- [ ] PR CI 执行语法、Node 测试、长截图、版本一致性和 Electron runtime probe。
- [ ] 生产 Release 强制签名，并验证发布者、时间戳、SHA-256、fuses 和 ASAR integrity。
- [ ] 真实 renderer smoke 捕获 console error、pageerror 和 render-process-gone。
- [ ] 所有窗口有统一安全选项、导航/新窗口守卫和最小 preload。
- [ ] 全部 IPC 完成 sender 与窗口 owner 审计。
- [x] 可导出默认离线且经过 canary secret 扫描的诊断包。
- [ ] NSIS 与 portable 均完成打包、启动和退出烟测。
- [ ] 从 2.0.0 覆盖安装后配置、历史、API Key 和数据目录保持不变。
- [ ] Windows 10/11、100%/125%/150%/200% DPI、混合 DPI 多屏、睡眠恢复和 RDP 完成验收。
- [ ] 公开 Beta 与正式版本使用独立版本提交、匹配的 `v*` tag 和不可覆盖的 Release 资产。

## 6. 变更与提交纪律

- 每个路线目标使用独立提交；安全、重构、发布和功能工作不得混在同一提交。
- 任一范围变更先修改本文并单独提交，说明用户价值、风险、验证方法和延期代价。
- 构建产物不进入源码提交；Release 资产由受保护的发布流程生成。
- 任何门禁失败都保留当前可运行版本，不通过重写远端历史隐藏失败。

## 7. 发布门禁实施记录

2026-08-09 在目标 2 提交前完成 hardened unsigned 候选构建，用于证明正式配置可以在没有证书时执行除签名外的同一组门禁：

| 产物 | 字节 | SHA-256 |
|---|---:|---|
| `Highlighter-Setup-2.1.0-beta.0.exe` | 151,560,864 | `44C4E7DD3E638FD93402CDC654D7A50BEBCEA08EC86A4311B25A281D32A62B56` |
| `Highlighter-2.1.0-beta.0-portable.exe` | 151,292,319 | `FDD3AFCE2F83E79C4E74BB06BCF5A6E51E703E7D70DA0ED38ABF38CE7E5D8145` |

验证器同时确认 blockmap、`beta.yml`、CycloneDX SBOM 和 8 项 Electron production fuses，并生成 `SHA256SUMS.txt`。该候选未签名、未安装且未发布，只是本地门禁验证证据，不能作为用户可用 Release。

## 8. 本地诊断实施记录

2026-08-09 完成本地诊断闭环：应用日志改为带 session id、版本和事件类型的结构化记录；runtime marker 可在下次启动识别未正常退出，并在正常退出时原子标记 clean。Electron crash reporter 只写本地受管目录且禁用上传。

系统设置页可预览并导出 ZIP。默认白名单仅包含应用/运行时/Windows/显示器摘要、原生组件版本与 SHA-256、轮转日志和最近进程退出记录；不读取配置原文、凭据、AI/划词内容、截图历史、OCR 或录屏媒体。crash dump 默认排除，只有用户勾选并确认后才加入。

自动测试使用假的 API Key、Bearer token、prompt、AI content、用户名路径和数据根路径反解扫描 ZIP；真实 Electron 主进程烟测同时确认 `uploadToServer=false`、设置页安全 IPC、ZIP 可解压以及正常退出 marker。

## 9. 依赖供应链审计记录

2026-08-09 对运行时、原生模块和 Windows 构建链执行完整 `npm audit`。`sharp` 固定为 `0.35.3`，`@electron/rebuild` 固定为 `4.2.0`，`onnxruntime-node` 固定为 `1.27.0`；由于 ONNX 上游仍声明易受攻击的 `adm-zip ^0.5.16`，项目将其精确覆盖到已修复的 `0.6.0`。同时刷新 `tar`、`js-yaml`、`undici`、`fast-uri` 和 `brace-expansion` 等传递依赖。

干净锁文件解析后的完整审计结果为 0 项漏洞（含 production、development、optional 和 build dependencies），生产依赖审计同样为 0 项。`npm run audit:dependencies` 已加入 PR CI 和签名 Release，在 moderate 及以上漏洞出现时阻止构建；离线锁文件测试另行约束关键修复版本，防止上游范围回退。
